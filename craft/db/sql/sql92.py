#!/usr/bin/env python
# -*- coding: utf-8 -*-
__author__ = 'Michal Szczepanski'

from sqlalchemy.types import SMALLINT, INTEGER, INT, BIGINT, DECIMAL, NUMERIC, FLOAT, REAL
from sqlalchemy.types import CHAR, NCHAR, VARCHAR, NVARCHAR
from sqlalchemy.types import DATE, DATETIME, TIMESTAMP, TIME
from sqlalchemy.types import BLOB, BINARY, VARBINARY
from sqlalchemy.types import BOOLEAN
from sqlalchemy.types import TEXT, CLOB

types = [
    SMALLINT,
    INTEGER,
    INT,
    BIGINT,
    DECIMAL,
    NUMERIC,
    FLOAT,
    REAL,
    CHAR,
    NCHAR,
    VARCHAR,
    NVARCHAR,
    DATE,
    DATETIME,
    TIMESTAMP,
    TIME,
    BLOB,
    BINARY,
    VARBINARY,
    BOOLEAN,
    TEXT,
    CLOB
]
